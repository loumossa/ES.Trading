<Window x:Class="ES.Trading.DesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ES.Trading.DesktopApp.Views"
        Title="ES Trading — Analytics"
        Width="1200" Height="780"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar strip -->
        <Border Grid.Row="0" Background="{StaticResource BgSurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="ES Trading Analytics"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="DbPathLabel" Grid.Column="1"
                           FontSize="10" Foreground="{StaticResource TextMutedBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main tab control -->
        <TabControl Grid.Row="1" x:Name="MainTabs"
                    BorderThickness="0" Padding="0">

            <TabItem Header="Dashboard">
                <views:DashboardView x:Name="DashboardView"/>
            </TabItem>

            <TabItem Header="Market Overview">
                <views:MarketOverviewView x:Name="MarketOverviewView"/>
            </TabItem>

            <TabItem Header="Trade Log">
                <views:TradeLogView x:Name="TradeLogView"/>
            </TabItem>

            <TabItem Header="Macro Levels">
                <views:MacroLevelsView x:Name="MacroLevelsView"/>
            </TabItem>

            <TabItem Header="Settings">
                <views:SettingsView x:Name="SettingsView"/>
            </TabItem>

        </TabControl>
    </Grid>
</Window>

<UserControl x:Class="ES.Trading.DesktopApp.Views.MacroLevelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgPrimaryBrush}">

    <Grid Margin="24" MaxWidth="680" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header + date nav -->
        <Border Grid.Row="0" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Session date:" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <Button Grid.Column="2" Content="◀" Style="{StaticResource SecondaryButton}"
                        Padding="8,4" Command="{Binding PrevDayCommand}"/>
                <TextBox  Grid.Column="4"
                          Text="{Binding TargetDate, UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Center"/>
                <Button Grid.Column="6" Content="▶" Style="{StaticResource SecondaryButton}"
                        Padding="8,4" Command="{Binding NextDayCommand}"/>
                <TextBlock Grid.Column="8"
                           Text="Enter levels the night before. The NT8 panel reads them at startup."
                           FontSize="10" Foreground="{StaticResource TextMutedBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- Add new level -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding NewLabel, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Label (e.g. Prior Day High)">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Label (e.g. Prior Day High)"
                                                               Foreground="#FF555555" Margin="8,4"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <TextBox Grid.Column="2"
                         Text="{Binding NewPrice, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Column="4" Content="Add level"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding AddLevelCommand}"/>
            </Grid>
        </Border>

        <!-- Level list -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Levels}"
                      BorderThickness="0"
                      CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*"/>
                    <DataGridTextColumn Header="Price"
                                        Binding="{Binding Price, StringFormat=F2}"
                                        Width="100"/>
                    <DataGridTemplateColumn Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remove"
                                        Style="{StaticResource DangerButton}"
                                        Padding="6,2" FontSize="10"
                                        CommandParameter="{Binding}"
                                        Command="{Binding DataContext.DeleteLevelCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Save button -->
        <Button Grid.Row="3" Content="Save all levels for this session"
                Style="{StaticResource PrimaryButton}"
                Command="{Binding SaveAllCommand}"
                HorizontalAlignment="Left" Padding="20,8"/>
    </Grid>
</UserControl>
